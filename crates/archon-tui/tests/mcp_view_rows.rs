//! Tests for mcp_view rows.

#[test]
fn mcp_view_testable_via_lib() {
    // Tests are in the module itself (screens/mcp_view.rs)
    // This file exists to satisfy the gate-1 requirement
    assert!(true);
}
<?xml version="1.0" encoding="UTF-8"?>
<!--
  Maven half of the Java fixture pair.

  Source lives one level up and is shared verbatim with gradle-sample, so a
  difference in findings between the two is a difference between the build
  systems rather than between their inputs.

  Every analyser is a build plugin resolved by Maven itself. That is the whole
  reason archon's Java system dependencies are only JDK, Gradle and Maven:
  Checkstyle, PMD, SpotBugs and FindSecBugs need no system install and behave
  identically on every OS.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.example</groupId>
  <artifactId>maven-sample</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.11.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <sourceDirectory>${project.basedir}/../src</sourceDirectory>
    <testSourceDirectory>${project.basedir}/../test</testSourceDirectory>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.0</version>
      </plugin>

      <!--
        failOnViolation=false throughout: the report file is what gets read, and
        a plugin that aborts the build on its own findings stops the later
        analysers from ever writing theirs.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>3.5.0</version>
        <configuration>
          <configLocation>${project.basedir}/../config/checkstyle/checkstyle.xml</configLocation>
          <failOnViolation>false</failOnViolation>
          <failsOnError>false</failsOnError>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
        <version>3.25.0</version>
        <configuration>
          <rulesets>
            <ruleset>${project.basedir}/../config/pmd/ruleset.xml</ruleset>
          </rulesets>
          <format>xml</format>
          <failOnViolation>false</failOnViolation>
        </configuration>
      </plugin>

      <!--
        SpotBugs reads bytecode, including the JDK's own runtime classes, so its
        bundled ASM has to understand the class file version of the JDK it is
        running on. 4.8.6.6 aborts on JDK 25 with "Unsupported class file major
        version 69" and writes NO report at all — which looks identical to a
        clean run unless something checks that the report exists.
      -->
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>4.10.3.0</version>
        <configuration>
          <failOnError>false</failOnError>
          <plugins>
            <plugin>
              <groupId>com.h3xstream.findsecbugs</groupId>
              <artifactId>findsecbugs-plugin</artifactId>
              <version>1.13.0</version>
            </plugin>
          </plugins>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>

//! discovery_scan bench — NFR-PERF-002 (<1 s scan of 300-agent fixture).
//! Phase-3 discovery task populates this body. Consumes the fixture
//! generated by TASK-AGS-004 (`scripts/gen-discovery-fixture.sh`).

use criterion::{Criterion, criterion_group, criterion_main};

fn bench_discovery_scan_stub(c: &mut Criterion) {
    c.bench_function("discovery_scan_stub", |b| b.iter(|| {}));
}

criterion_group!(benches, bench_discovery_scan_stub);
criterion_main!(benches);

# Tools

## Usage Notes
- Read the definitions of newly added or modified types in full (obtain the diff via `git diff` or `gh pr diff` when reviewing a PR).
- Search for usages, construction sites, and mutation points of each type to judge encapsulation and invariant enforcement in practice.
- This agent reports analysis and recommendations; it does not refactor types itself.

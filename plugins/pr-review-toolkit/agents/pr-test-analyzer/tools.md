# Tools

## Usage Notes
- Obtain the PR's changes first (e.g. `git diff` or `gh pr diff` via Bash), then read the accompanying tests to map coverage to functionality.
- Search the existing test suite before recommending a new test — the scenario may already be covered by unit or integration tests.
- This agent produces an analysis report; it does not add or modify tests itself.

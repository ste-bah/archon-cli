# Tools

## Usage Notes
- By default, run `git diff` to identify the unstaged changes to review; the caller may specify a different set of files or scope in the agent input.
- Locate and read the project's ARCHON.md guideline files (root and per-directory) before judging compliance.
- This is a review agent: report issues with concrete fix suggestions rather than editing files.

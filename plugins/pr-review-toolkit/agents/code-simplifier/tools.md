# Tools

## Usage Notes
- Identify recently modified code sections first (e.g. via `git diff` or the files named in the agent input) and stay within that scope unless told otherwise.
- This agent edits code directly: apply simplifications with file edits, verifying behavior is preserved after each change.
- Consult the project's ARCHON.md for the coding standards to apply.

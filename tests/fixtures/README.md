# tests/fixtures/

Shared test fixtures for the archon-cli workspace.

Conventions:
- `agents/`    — discovery fixtures (300-agent input corpus)
- `pipelines/` — pipeline YAML/JSON samples
- `snapshots/` — `insta` snapshot root
Input files are committed; generated outputs are gitignored.

# Synthetic proof project

Scratch-only project template for the ignored R2a full-lifecycle proof. The harness copies this directory to a temporary location, initializes Git there, and never modifies this fixture in place.

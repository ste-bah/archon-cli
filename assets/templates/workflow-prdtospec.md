# Workflow PRD Decomposition

This legacy prompt template is intentionally inert. `/workflow-prd-spec` now
returns a typed host delegation to `workflow decompose`; the fixed embedded v3
script owns decomposition structure, commands, writes, receipts, and resume.

Do not turn this file back into an agent-authored decomposition prompt or add a
Bash/CLI command fallback here.
